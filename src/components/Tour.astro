
<script>
  import { driver } from "driver.js";
  import "driver.js/dist/driver.css";

  document.addEventListener("DOMContentLoaded", function () {
    const driverObj = driver({
      showProgress: true,
      showButtons: ["next", "previous", "close"],
      popoverClass: "blue-theme-popover",
      steps: [
        {
          element: "#main-title",
          popover: {
            title: "Welcome to Our Site",
            description:
              "This is the main title of our website. Let's take a tour of the key features!",
            side: "bottom",
            align: "start",
          },
        },
        {
          element: "#navigation-area",
          popover: {
            title: "Navigation Area",
            description:
              "Here you can find important navigation links and the tour button.",
            side: "bottom",
            align: "center",
          },
        },
        {
          element: "#feature-1",
          popover: {
            title: "Feature 1",
            description:
              "This is our first amazing feature. Click to learn more about what it can do!",
            side: "right",
            align: "start",
          },
        },
        {
          element: "#feature-2",
          popover: {
            title: "Feature 2",
            description:
              "Our second feature is equally impressive and user-friendly.",
            side: "left",
            align: "start",
          },
        },
        {
          popover: {
            title: "Tour Complete!",
            description: `
              That's the end of our tour. You can restart it anytime by clicking the "Start Tour" button.<br/><br/>
              <img src="https://res.cloudinary.com/yenvietsoft/image/upload/v1752051019/Rubito/learing_e14vx9.gif" 
                   alt="Tour Complete GIF" 
                   style="width:100%; border-radius:8px; box-shadow:0 4px 10px rgba(0,0,0,0.1);" />
            `,
          },
        },
      ],
    });

    const startTourButton = document.getElementById("start-tour");
    if (startTourButton) {
      startTourButton.addEventListener("click", function () {
        driverObj.drive();
      });
    }
  });
</script>

<style>
  :global(.blue-theme-popover.driver-popover) {
    max-width: 360px;
    max-height: 80vh;
    overflow-y: auto;
    background: #f0f6ff;
    border: 1px solid #93c5fd;
    border-radius: 10px;
    box-shadow: 0 8px 20px rgba(30, 64, 175, 0.15);
    padding: 1rem;
    scrollbar-width: thin;
    scrollbar-color: #93c5fd transparent;
  }

  @media (max-width: 480px) {
    :global(.blue-theme-popover.driver-popover) {
      max-width: 90vw;
      padding: 0.75rem;
    }
  }

  :global(.blue-theme-popover .driver-popover-title) {
    font-size: 1.25rem;
    font-weight: 600;
    color: #1e3a8a;
    margin-bottom: 0.5rem;
  }

  :global(.blue-theme-popover .driver-popover-description) {
    font-size: 0.95rem;
    line-height: 1.5;
    color: #1e40af;
    margin-bottom: 1rem;
  }

  :global(.blue-theme-popover .driver-popover-arrow) {
    border-color: #93c5fd;
  }

  :global(.blue-theme-popover .driver-popover-footer) {
    background: transparent;
    border-top: none;
    padding: 0;
    margin-top: 1rem;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  :global(.blue-theme-popover .driver-popover-progress-text) {
    color: #3b82f6;
    font-size: 0.85rem;
    font-weight: 500;
    text-align: center;
  }

  /* Animated Progress Bar */
  :global(.blue-theme-popover .driver-popover-progress-text)::after {
    content: "";
    display: block;
    margin-top: 6px;
    height: 6px;
    background: linear-gradient(to right, #3b82f6, #2563eb);
    border-radius: 4px;
    animation: progressBarAnim 2s ease-in-out infinite;
  }

  @keyframes progressBarAnim {
    0% {
      width: 10%;
      opacity: 0.3;
    }
    50% {
      width: 90%;
      opacity: 1;
    }
    100% {
      width: 10%;
      opacity: 0.3;
    }
  }
:global(.blue-theme-popover .driver-popover-prev-btn),
:global(.blue-theme-popover .driver-popover-next-btn) {
  background-color: #e0f2fe;
  color: #1e40af;
  border: 1px solid #bfdbfe;
  border-radius: 6px;
  padding: 4px 10px;
  font-size: 0.8rem;
  font-weight: 500;
  cursor: pointer;
  transition: all 0.2s ease;
  box-shadow: 0 1px 2px rgba(0, 0, 0, 0.05);
}

:global(.blue-theme-popover .driver-popover-prev-btn:hover),
:global(.blue-theme-popover .driver-popover-next-btn:hover) {
  background-color: #bae6fd;
  color: #1e3a8a;
  transform: translateY(-1px);
  box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
}


  :global(.blue-theme-popover .driver-popover-close-btn) {
    background: transparent;
    color: #1e3a8a;
    border: 1px solid #cbd5e1;
    border-radius: 50%;
    width: 26px;
    height: 26px;
    font-size: 14px;
    font-weight: bold;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: all 0.2s ease;
  }

  :global(.blue-theme-popover .driver-popover-close-btn:hover) {
    background: #e0e7ff;
  }

  :global(.driver-overlay) {
    background: rgba(30, 64, 175, 0.15);
  }

  :global(.driver-active-element) {
    border-radius: 8px;
    box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.4);
    transition: all 0.3s ease;
  }
</style>
